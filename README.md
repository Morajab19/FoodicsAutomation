# FoodicsAutomation

## Setup Instructions
1. Clone this repository.
2. Set your Amazon login credentials in `config.properties`.
3. Run `mvn clean install` to download dependencies.
4. Run tests by running TestRunner class
